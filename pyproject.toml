[project]
name = "xarray"
version = "0.0.1a1"
requires-python = ">= 3.10"
dependencies = [
    "typing_extensions>=4.5",
    "array_api_compat",
    "array_api_extra",
]

[build-system]
requires = ["setuptools"]

[tool.setuptools.packages.find]
include = ["*"]
exclude = ["training*", "tests*"]